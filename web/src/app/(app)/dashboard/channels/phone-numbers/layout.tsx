/**
 * Minimal route-level layout - NO IMPORTS
 * Temporarily sterile for debugging route hang issues.
 */
export default function PhoneLinesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

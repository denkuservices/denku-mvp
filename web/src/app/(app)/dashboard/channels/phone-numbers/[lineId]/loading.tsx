/**
 * Minimal loading state - NO IMPORTS
 * Temporarily sterile for debugging route hang issues.
 */
export default function PhoneLineDetailLoading() {
  return <div style={{ padding: 24 }}>Loading…</div>;
}

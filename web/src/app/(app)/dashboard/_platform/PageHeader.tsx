/**
 * Compatibility re-export: platform routes now share the same Horizon page header as every
 * authenticated surface. Keeping this path avoids a noisy route-by-route import migration.
 */
export { PageHeader as default } from "@/components/ui-horizon/page-header";
